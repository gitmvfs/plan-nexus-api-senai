name: Trigger auto deployment for plan-nexus-senai

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/plan-nexus-senai-AutoDeployTrigger-5850d41b-1f26-4a85-8f73-e628e2eb6202.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.PLANNEXUSSENAI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.PLANNEXUSSENAI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.PLANNEXUSSENAI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: acrplannexussenai.azurecr.io
          registryUsername: ${{ secrets.PLANNEXUSSENAI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.PLANNEXUSSENAI_REGISTRY_PASSWORD }}
          containerAppName: plan-nexus-senai
          resourceGroup: plan-nexus
          imageToBuild: acrplannexussenai.azurecr.io/plan-nexus-senai:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


